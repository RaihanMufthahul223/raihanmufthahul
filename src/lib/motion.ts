import type { Easing } from 'framer-motion';

// Persona 5 custom easing — sharp, decisive, fast-out
export const EASE_P5: Easing = 'easeOut';

// For use in transition objects
export const EASE_SHARP: [number, number, number, number] = [0.25, 0, 0, 1];
